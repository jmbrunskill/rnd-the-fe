export function render(outlet: HTMLElement) {
  outlet.innerHTML = `
    <section class="page">
      <h1>Stocktake</h1>
      <p>This is where the real stocktake screen gets built.</p>
    </section>
  `;
}
